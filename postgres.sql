CREATE TABLE bonsai (
	username VARCHAR(50),
	nickname VARCHAR(50),

)

CREATE TYPE food (
	quantity integer,
	
)

CREATE TYPE nutrient (
	calories integer,
	carbs integer,
	protein integer,
	fat integer
)